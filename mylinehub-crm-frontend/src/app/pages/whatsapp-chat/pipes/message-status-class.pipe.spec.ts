import { MessageStatusClassPipe } from './message-status-class.pipe';

describe('MessageStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
