import { HideStringFromEmployeePipe } from './hide-string-from-employee.pipe';

describe('HideStringFromEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new HideStringFromEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
